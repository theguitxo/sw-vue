import constants from '@/constants/dialog';

const {
  TYPES,
  DEFAULTS,
} = constants;

export default {
  show: false,
  errorDescription: '',
  errorType: TYPES.ERROR,
  dialogTitle: DEFAULTS.TITLE,
};
